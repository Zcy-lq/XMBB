# WeChat Build Checklist

## Current Status - 2026-06-01

- Automated logic gate: `verify_full_loop_acceptance` passes 60/60 checks.
- Release compliance code gate: `verify_release_compliance` passes locally and reports production-input blockers for AppID and legal URLs.
- Launch evidence gate: `verify_launch_evidence` passes local code mode but blocks final mode until `docs/launch_evidence/launch_evidence.json` records WeChat DevTools, full-loop runtime, UI signoff, real-device, and performance evidence.
- Cocos build output: PASS locally, `build/wechatgame` was generated by Cocos Creator 3.8.8 and `verify_wechat_build_output` passed for required files and package size.
- Cocos Creator executable: found at `C:\ProgramData\cocos\editors\Creator\3.8.8\CocosCreator.exe`.
- WeChat DevTools: installed and can launch, but CLI login/import automation times out, QR-output login is not produced, and no import/preview evidence is recorded.
- Launch status: not releasable until WeChat import/preview and real-device smoke evidence are recorded.

## Before Build

- `assets/configs/platform.json` exists.
- `reviewMode` is explicitly configured.
- AppID and ad unit IDs are not hard-coded in TypeScript.
- Runtime asset configs exclude development reference images.
- Bundle and remote resource versions are documented.
- `docs/launch_evidence/launch_evidence.template.json` is copied to `launch_evidence.json` only after real evidence is collected.

## Cocos Build

- Target platform: WeChat Mini Game.
- Main package target is documented.
- Subpackage or remote resource strategy is documented.
- Build output has no missing asset errors.

## WeChat DevTools

- Import build output.
- Confirm launch screen appears.
- Confirm mock/fallback platform calls do not crash.
- Confirm privacy and agreement entries are accessible.
- Confirm one basic play loop reaches settlement.
- Save screenshots/logs into `docs/launch_evidence` or record stable external URLs in `launch_evidence.json`.

## Submission

- `docs/QA_BLOCKERS.md` has no P0 entries.
- P1 entries have Owner Agent and repair plan.
- `docs/WECHAT_RELEASE_COMPLIANCE.md` is current.
- Any unverified editor/devtools/real-device claims are explicitly marked UNVERIFIED.
- `XMBB_RELEASE_FINAL=1 node tools/verify_launch_evidence.mjs` exits with code `0`.
